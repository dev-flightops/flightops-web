/**
 * Placeholder copy shown in the dispatch packet until crew-service
 * ships flight_assignments (M3). Pinned to the demo tenant's chief
 * pilot from `scripts/seed_demo.py` so the packet reads consistently
 * with everything else the audience sees (roster, elog CP-review
 * queue) instead of surfacing a name that appears nowhere else.
 */
export const DEMO_PIC_NAME = "Sarah Kessler";
export const DEMO_PIC_CERT = "ATP-CFI-0058291";
