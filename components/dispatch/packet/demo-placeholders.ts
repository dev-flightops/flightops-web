/**
 * Placeholder copy shown in the dispatch packet until the supporting
 * services ship. Mirrors the legacy demo-seed pilot/cert so the
 * post-selection screenshot matches the real product (`Brian Larson`
 * + `DEMO-CERT-1-005`).
 *
 * Real values come from crew-service in M3.
 */
export const DEMO_PIC_NAME = "Brian Larson";
export const DEMO_PIC_CERT = "DEMO-CERT-1-005";
